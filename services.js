/**
 * services.js
 * 
 * Este arquivo contém integrações com APIs externas, como serviços de clima
 * ou modelos de IA generativa.
 * 
 * As funções aqui são placeholders e podem ser substituídas por implementações reais.
 */

/**
 * Busca a previsão do tempo para uma cidade. (Placeholder)
 */
export async function getWeather(city) {
    return `🌦️ O comando de clima para "${city}" ainda está em desenvolvimento.`;
}

/**
 * Obtém uma resposta de um modelo de IA generativa. (Placeholder)
 */
export async function getGeminiResponse(prompt) {
    return `🤖 A integração com o Gemini para a pergunta "${prompt}" ainda está em desenvolvimento.`;
}